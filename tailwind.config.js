/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,ts,jsx,tsx}", // ✅ Leave as-is — don't change to @/
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  